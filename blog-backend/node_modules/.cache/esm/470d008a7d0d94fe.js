let Koa,Router,bodyParser,mongoose,api,createFakeData;_7d0‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_7d0‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_7d0‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_7d0‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_7d0‍.w("./api",[["default",["api"],function(v){api=v}]]);_7d0‍.w("./createFakeData",[["default",["createFakeData"],function(v){createFakeData=v}]]);require('dotenv').config();








const { PORT, MONGO_URI } = process.env;

mongoose
    .connect(MONGO_URI, { useNewUrlParser: true, useFindAndModify: false })
    .then(() => {
        console.log('Connect to MongoDB');
        createFakeData();
    })
    .catch(e => {
        _7d0‍.g.console.error(e);
    });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes()); // api 라우트 적용

// 라우터 설정
app.use(bodyParser());
// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
    _7d0‍.g.console.log('listening to port %d', port);
});